# Empty = relative API base. The production CloudFront distribution proxies
# /api/* to the backend, so same-origin requests work on every hostname
# (gridironlab.live, www.gridironlab.live, d1mkvupgst3eb9.cloudfront.net).
VITE_API_URL=
